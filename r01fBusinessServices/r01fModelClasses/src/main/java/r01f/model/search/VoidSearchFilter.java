package r01f.model.search;

import r01f.model.metadata.annotations.ModelObjectData;

/**
 * {@link SearchFilter} type to be used when annotating a NOT searchable model object
 * with {@link ModelObjectData}
 */
public interface VoidSearchFilter
	     extends SearchFilter {
	/* just a marker interface */
}
