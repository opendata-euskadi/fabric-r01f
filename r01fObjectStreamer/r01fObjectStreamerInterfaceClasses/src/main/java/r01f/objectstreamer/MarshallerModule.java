package r01f.objectstreamer;

/**
 * Just marker interface that custom jackson modules must implement
 * (it's used to avoid the dependency on jackson-core on this interface project)
 */
public interface MarshallerModule {
	// just a marker interface
}
