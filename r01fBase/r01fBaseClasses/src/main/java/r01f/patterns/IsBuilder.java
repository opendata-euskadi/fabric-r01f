package r01f.patterns;

/**
 * Implement this interface at builder types: the ones with static create() kind methods
 * this way all Builder types could be located easily
 */
public interface IsBuilder {
	// just a marker interface for builder types
}
